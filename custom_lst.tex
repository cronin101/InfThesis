\lstset{
    backgroundcolor=\color{yellow!5!blue!3},%
    commentstyle = \ttfamily\color{black!50},
    keywordstyle=\ttfamily\color{blue!60!black},
    stringstyle=\color{red!60!black},
    basicstyle=\footnotesize\ttfamily,%
    showstringspaces=false,
    numbers=left, numberstyle=\tiny, stepnumber=4, numbersep=5pt%
}
%\lstset{emph={%  
%    module, define_method, end, else, sample, elsif, if, repeat, until, emit, abs, give, neighbour, me, len, with, simplex, maximize, NONNEGATIVE, for, in, solve, add, next, unless,provider,GUID,blip_body,tags,%
%    show, class, def, from, import, plot, return
%    },emphstyle={\color{red!60!black}\bfseries}%
%}%

